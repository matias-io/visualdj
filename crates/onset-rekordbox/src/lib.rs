//! Read-only access to rekordbox's local data: paths, the SQLCipher-encrypted `master.db`,
//! the collection model built from it, and the ANLZ analysis files (beat grid, phrases, cues).
#![forbid(unsafe_code)]
